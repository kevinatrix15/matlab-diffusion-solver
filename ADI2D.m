classdef ADI2D
  properties
    A1
    A2
  end
  methods
    function A = buildLHSMatrix(alph, dt, dx, dy)
    end
    function E = buildRHSMatrix(alph, dt, dx, dy)
    end
  end
end
function [] = CrankNicolson2D()


